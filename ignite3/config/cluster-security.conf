# Default security is applied by scripts/ignite3-init.sh after cluster init:
#   1) set ignite user password from IGNITE_LAB_PASSWORD
#   2) enable ignite.security.enabled=true
#
# Optional HOCON for --config-files at init time (schema varies by patch level):
ignite {
  security {
    enabled = false
  }
}
