---
apiVersion: v1
kind: Secret
metadata:
  name: ignite3-auth
  labels:
    app.kubernetes.io/name: ignite-lab
    app.kubernetes.io/instance: ignite3
    app.kubernetes.io/version: "3.1.0"
type: Opaque
stringData:
  # Default lab credential. Override with auth.password or auth.existingSecret.
  password: "ignite-lab-pass"
